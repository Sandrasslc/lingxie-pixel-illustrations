---
name: lingxie-pixel-illustrations
description: "从文章、活动策划、更新日志等完整内容中提炼视觉叙事，生成灵蟹（SoloEnt）品牌的像素风 Banner、文章头图、活动 KV、社交媒体配图或海报。适用于明确要求灵蟹像素风、8/16-bit 游戏美术感或像素螃蟹 mascot 的任务；不用于普通手绘线稿配图，除非用户明确要求混合风格。"
---

# 灵蟹像素风配图

把一段有信息量的内容转成一张能独立传达主题的品牌配图。画面尺寸、场景、背景和构图随发布场景与输入内容变化，不套用固定画布，也不把奶油白底当作必选项。

## 开始前

每次生成前都阅读 [references/brand-visual-system.md](references/brand-visual-system.md)。构造生成提示词时，再阅读 [references/prompt-template.md](references/prompt-template.md)。

检查 `assets/` 中的品牌参考图，并按以下角色使用：

- `lingxie-logo-reference.png`：Logo 的几何结构、蟹红配色与品牌识别依据。
- `weekly-banner-zh-reference.png`：中文信息层级、左上角品牌锁定区、周报主题表达参考。
- `weekly-banner-en-reference.png`：英文信息层级、品牌锁定区与横版构图参考。

两张周报 Banner 是内容层级和品牌位置参考，不是像素画风参考。不要复制其中的手绘线条、纸张场景或固定办公室叙事。

## 工作方式

### 1. 从正文提炼视觉任务

先完整阅读用户提供的文章、活动策划、更新日志或周报，整理一份内部视觉简报：

- 发布用途与受众
- 一句话核心信息
- 3 至 5 个最值得视觉化的内容锚点
- 用户必须保留的标题、日期、口号或行动信息
- 希望读者产生的第一印象或行动

不要把全文逐条塞进画面。选择一个主叙事和少量辅助符号，使用户先看懂主题，再看到细节。

### 2. 决定尺寸与信息密度

用户指定尺寸或比例时严格遵守。未指定时，根据最终用途推断比例，不要机械默认某个尺寸：

- 网站或文章头图通常用横版 16:9、3:2 或更宽的 Header 比例。
- 方形社交卡片通常用 1:1。
- 竖版海报或信息流封面通常用 4:5 或 3:4。
- 演示文稿或视频封面通常用 16:9。

把选定比例写进生成提示词。若最终文件必须达到精确像素尺寸，先生成匹配比例的画面，再做非破坏裁切，并用 nearest-neighbor 缩放保持像素边缘，禁止平滑插值。

### 3. 让场景来自内容

把抽象内容转成一个有动作的像素世界，而不是复用固定办公室或固定白底。可把功能更新转成装备工坊、版本传送带或升级关卡；把活动策划转成集结点、地图关卡或舞台；把文章观点转成与主题直接相关的场景隐喻。

螃蟹 mascot 必须参与叙事，例如操作、搬运、探索、发布、协作或庆祝，不能只是贴在角落的装饰。每张图只选一个主场景，避免拼贴式堆满图标。

### 4. 应用品牌不变量

无论背景如何变化，都要同时保留：

- 蟹红或珊瑚红为主识别色
- 黑色像素描边
- 清晰可见的 8-bit 或 16-bit 游戏美术语言
- 可识别的灵蟹像素 Logo/品牌锁定区
- 与内容互动的像素螃蟹 mascot

优先保留周报参考图中“左上角小型品牌锁定区”的思路：紧凑、清晰、有留白，不与主标题争夺层级。画幅太窄时可改成角标、状态栏或游戏 HUD，但不能丢失品牌识别。

### 5. 生成

使用内置 `image_gen` 生成位图。把所有参考图逐一标明用途，避免模型把参考 Banner 当作要复刻的画面：

- Logo 图是 identity/shape reference。
- 周报图是 hierarchy/layout reference only。
- 用户新增图片要明确标成 style reference、subject reference 或 edit target。

一个资产或一个变体对应一次生成调用。除非用户明确要求保留旧画面并只修改局部，否则按“参考生成”处理，而不是编辑旧 Banner。

图中文字只保留必要层级：通常为一个标题、一个短副标题，以及最多 3 至 5 个极短标签。所有必需文案逐字引用，不自行改写品牌名、日期、数字、网址或活动名称，也不把正文段落交给图像模型排版。

### 6. 检查并定向修正

生成后必须查看原图并按 [品牌验收清单](references/brand-visual-system.md#验收清单) 检查。重点确认：

- 场景确实由本次内容推导，不是通用科技背景或旧场景复刻。
- Logo、螃蟹轮廓和品牌名可识别。
- 像素块、阶梯边缘和有限色阶在 100% 视图下成立，没有平滑矢量感、3D 塑料感或水彩笔触。
- 蟹红/珊瑚红和黑色描边仍是视觉主轴，其他颜色没有抢走品牌识别。
- 所有文字逐字正确，尤其是“灵蟹”“SoloEnt”、日期和数字。
- 缩略图状态下仍能看懂主标题与主动作。

若有问题，只做一次一个目标的修正，并重复不变量。文字无法稳定生成时，优先减少图中文字、保留干净标题区，再进行精确排版；绝不交付错字。

### 7. 交付

把最终选定图片保存到用户指定位置；项目内使用的图必须放入项目目录，不要只留在临时生成目录。交付时说明：

- 文件路径
- 实际尺寸或比例
- 使用的标题文案
- 最终生成提示词
- 参考图各自承担的角色

除非用户要求，不需要附带失败草稿或多余中间图。
